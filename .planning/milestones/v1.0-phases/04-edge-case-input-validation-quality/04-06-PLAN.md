---
phase: 04-edge-case-input-validation-quality
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05"]
files_modified:
  - SKILL.md
  - scripts/discovery-surfaces.test.mjs
autonomous: true
requirements: [DISC-04, DISC-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`## Case generation protocol` instructs the orchestrator to emit at least one permission case per discovered RLS policy record and per discovered in-code role guard, from both detection paths run independently (D-03, D-04, D-05)"
    - "An empty `policies` array is reported as 'no RLS policy matched this parser', never as a claim that the project has no permission boundaries — and the in-code role-guard pass runs regardless of whether any policy was found (D-03)"
    - "Whether a permission case is written runnable or pending is decided from a presence-only check of `QA_AGENT_TOKEN_SECONDARY`, in the exact exit-code-only shape `SKILL.md` documents — the variable's value is never read, printed or echoed (D-01, D-02)"
    - "A permission case records the execution layer the discovered check lives in, and where the pending shape plan 04-04 implemented cannot carry it, the protocol requires the layer to be named in the case title instead — D-04's record survives the pending state either way (D-02, D-04)"
    - "`## Running generated cases` step 4 refuses to dispatch a pending case by name, reports the document's own reason and names `QA_AGENT_TOKEN_SECONDARY`, and stops for that case — never attempted, never silently skipped, never counted as a test failure (D-02)"
    - "A permission case that acts as the second user dispatches `api-client.mjs --secondary`, and D-05's role delta is the observed difference between two runs of the same case whose `auth.credential` names which credential produced each (D-01, D-05)"
    - "A wrong-type or out-of-range case's expected result is the generic 'debe rechazar la request', never a specific HTTP status the code was never read to confirm (D-14)"
    - "Injection-style payloads are never generated by any rule in this protocol, and the protocol names `security-audit` as where that concern lives (D-13)"
    - "Generating a document still never runs a case in the same turn: the closing summary may offer to run them, and the developer must still say yes (D-06, D-07)"
  prohibitions:
    - statement: "MUST NOT generate a case containing an SQL-injection string, an XSS payload or any other attack payload — 'wrong type / out of range' is strictly a data-contract violation, and injection testing is explicitly out of this agent's scope"
      category: scope_boundary
      requirement_id: DISC-05
    - statement: "MUST NOT read, print or echo `QA_AGENT_TOKEN_SECONDARY`'s value to decide whether a case is runnable — presence is the only fact needed and the only fact permitted to be observed"
      category: credential_isolation
      requirement_id: DISC-05
    - statement: "MUST NOT dispatch a case marked pending, and MUST NOT report a refused pending case as a failed test — a case the run could not execute is a configuration fact, not a verdict about the application"
      category: honesty
      requirement_id: DISC-05
  artifacts:
    - path: "SKILL.md"
      provides: "Permission-case generation rules, the type-implied/wrong-type/out-of-range rules, and the pending-refusal plus `--secondary` dispatch wiring in `## Running generated cases`"
      contains: "no RLS policy matched this parser"
    - path: "scripts/discovery-surfaces.test.mjs"
      provides: "PERMISSION_GENERATION_MARKERS, TYPE_AND_RANGE_MARKERS and PENDING_DISPATCH_MARKERS plus their SKILL.md anti-drift lock blocks"
      contains: "PERMISSION_GENERATION_MARKERS"
  key_links:
    - from: "SKILL.md"
      to: "scripts/discover-schema.mjs"
      via: "`## Case generation protocol` generates permission cases from the top-level `policies` array plan 04-02 added"
      pattern: "policies"
    - from: "SKILL.md"
      to: "references/discovery-nextjs.md"
      via: "`## Case generation protocol` defers in-code role-guard detection to the rubric plan 04-03 wrote"
      pattern: "discovery-nextjs\\.md"
    - from: "SKILL.md"
      to: "scripts/test-case-doc.mjs"
      via: "`## Running generated cases` step 4 dispatches on the `Ejecución` value plan 04-04's reader parses, and refuses the pending one"
      pattern: "Ejecución"
    - from: "SKILL.md"
      to: "scripts/api-client.mjs"
      via: "a secondary-role permission case dispatches through the `--secondary` flag plan 04-05 added"
      pattern: "--secondary"
    - from: "SKILL.md"
      to: "scripts/discovery-surfaces.test.mjs"
      via: "the SKILL.md protocol-agreement lock blocks, which fail when the protocol no longer states a rule the test names"
      pattern: "SKILL_PATH"
---

<objective>
Wire this phase's five other plans together at the only point where they become a behaviour a
developer can see: the orchestrator's own protocol. `## Case generation protocol` learns to
emit permission cases from the RLS policy records (04-02) and the in-code role guards (04-03),
to decide runnable-versus-pending from a presence-only credential check (04-05's contract), and
to write the pending shape 04-04 implemented. `## Running generated cases` learns to refuse a
pending case by name and to dispatch a secondary-role case through `--secondary`. The
wrong-type, out-of-range and type-implied rules DISC-05 asks for land in the same protocol.

Purpose: every prior plan in this phase produces an input — a bounds object, a value set, a
policy record, a detection rubric, a document state, a CLI flag — and none of them generates a
single permission case. This plan is the step where DISC-05's "genera casos de permisos/auth"
actually happens, and the step where every forward reference the other five plans make to the
generation and dispatch wiring resolves.

Output: three new rule groups in `SKILL.md` (`## Case generation protocol`) and a rewritten
step 4 plus closing note in `## Running generated cases`, each locked against drift by an
assertion block in `scripts/discovery-surfaces.test.mjs`.

**Phase goal (verbatim from ROADMAP.md):** Test cases the agent generates systematically
cover input-validation boundaries and negative/permission edge scenarios, grounded in the
constraints discovered in Phase 3, not just the happy path.
*(ROADMAP's `**Goal:**` line for this phase is not in `As a / I want to / so that`
user-story form; per MVP-mode rules no story is invented here.)*
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-edge-case-input-validation-quality/04-CONTEXT.md
@.planning/phases/04-edge-case-input-validation-quality/04-RESEARCH.md
@.planning/phases/04-edge-case-input-validation-quality/04-PATTERNS.md
@.planning/phases/04-edge-case-input-validation-quality/04-02-SUMMARY.md
@.planning/phases/04-edge-case-input-validation-quality/04-03-SUMMARY.md
@.planning/phases/04-edge-case-input-validation-quality/04-04-SUMMARY.md
@.planning/phases/04-edge-case-input-validation-quality/04-05-SUMMARY.md
@SKILL.md
</context>

## Artifacts this phase produces (plan 04-06 slice)

| Symbol / path | Kind | Where |
|---|---|---|
| Permission-case generation rules (policy + role-guard, provenance, pending decision) | new `## Case generation protocol` bullets | `SKILL.md` |
| Type-implied, wrong-type and out-of-range case rules (D-12/D-13/D-14) | new `## Case generation protocol` bullets | `SKILL.md` |
| Pending-case dispatch refusal and `--secondary` dispatch | rewritten step 4 of `## Running generated cases` | `SKILL.md` |
| The run offer (D-07) | amended closing bullet and closing note | `SKILL.md` |
| `PERMISSION_GENERATION_MARKERS` | new exported marker list | `scripts/discovery-surfaces.test.mjs` |
| `TYPE_AND_RANGE_MARKERS` | new exported marker list | `scripts/discovery-surfaces.test.mjs` |
| `PENDING_DISPATCH_MARKERS` | new exported marker list | `scripts/discovery-surfaces.test.mjs` |

*(This plan writes no new parser, no document-schema change and no new script. Every input it
consumes was built by an earlier plan in this phase: bounds and the boundary rule in 04-01,
`allowedValues` and `policies` in 04-02, the role-guard and format rubric in 04-03, the pending
`Ejecución` state in 04-04, the `--secondary` flag and the credential evidence field in 04-05.)*

<tasks>

<task type="auto">
  <name>Task 1: Permission-case generation — one case per discovered policy and per discovered role guard</name>
  <reversibility rating="costly">These bullets are the contract every permission case in every target project is generated from, and 04-02's and 04-03's detection shapes were both rated costly for the same reason — reshaping the generation rule later means re-deriving the policy record, the rubric and this protocol together.</reversibility>
  <precondition>Plans 04-02, 04-03, 04-04 and 04-05 are committed, so the `policies` array, the role-guard rubric section, the pending `Ejecución` shape and the `--secondary` contract all exist and can be named as they were actually implemented rather than as this plan predicted them.</precondition>
  <files>SKILL.md, scripts/discovery-surfaces.test.mjs</files>
  <read_first>
    - `SKILL.md` `## Case generation protocol` in full, as committed by plans 04-01 and 04-05 — the one-section-per-surface bullet, the one-case-per-observed-outcome bullet, the `Ejecución` bullet (which this task extends for a third case of surface), the `Tipo` negativo-versus-edge bullet with its worked contrast, the coverage-honesty bullet 04-01 extended, the no-literal-credential bullet, the Write bullet, the validate-by-parsing-back bullet, and the closing terminal-step bullet.
    - `SKILL.md` `## Discovery protocol` steps 6 through 12 — what is actually handed to the generation protocol, and step 10's stay-inside-the-root rule.
    - `references/discovery-nextjs.md` `## Permission / role-guard detection` as committed by plan 04-03 — the primary pattern, the two worked examples, and the exact wording of its closing finding-nothing rule, which this protocol must cite rather than restate.
    - `04-02-SUMMARY.md` and `scripts/discover-schema.mjs` as committed — the real field names on each `policies` record (`policyName`, `table`, `command`, `role`, `using`, `withCheck`, `source`) and the `policyWithCheckCount` key, so the protocol names what the JSON actually carries.
    - `04-04-SUMMARY.md` and `references/test-case-format.md` as committed — which option was selected at 04-04's decision checkpoint and the exact pending shape it produced; this task must write the shape that exists, not a shape it assumed.
    - `SKILL.md` `## Configuration` as committed by plan 04-05 — the `QA_AGENT_TOKEN_SECONDARY` entry and the presence-check-only rule this task's bullets defer to.
    - `.planning/phases/04-edge-case-input-validation-quality/04-CONTEXT.md` — D-01, D-02, D-03, D-04, D-05 verbatim.
    - `.planning/phases/04-edge-case-input-validation-quality/04-RESEARCH.md` — Pitfall 3 in full (empty is not absent, with the DATAX-web evidence), Pitfall 4 (presence not value), and the system-architecture diagram's Case-generation box.
    - `scripts/discovery-surfaces.test.mjs` — the file header explaining it is a doc proof and not part of the runtime path, the `SKILL_PATH` constant, the `--- Pattern constants ---` block, and the `describe('pattern doc agreement (anti-drift lock)', ...)` block including its `states every excluded directory, in the doc and in SKILL.md` test, which is the closest analog for a SKILL.md-text lock.
  </read_first>
  <action>
Add a permission-case group to `SKILL.md`'s `## Case generation protocol`, positioned after
the existing `Tipo` bullet and before the coverage-honesty bullet, as a small set of bullets in
the section's established voice (a rule, then the specific thing never to do, and why).

State that permission-case generation reads two independent inputs and that neither one's result
is evidence about the other: the top-level `policies` array from `discover-schema.mjs`'s JSON,
and the in-code role guards found by `references/discovery-nextjs.md`'s
`## Permission / role-guard detection` rubric. Both passes always run. At least one permission
case is generated per policy record and per detected role guard — a policy that guards three
commands is not collapsed into one case, and two handlers guarding the same role are two cases,
matching the existing one-case-per-observed-outcome discipline rather than one case per table.

State the provenance rule explicitly, because it is the whole mitigation for reading silence as
coverage: each permission case names which pass found it and cites its source — a policy case
cites the migration file and line from the record's `source`, quoting the `withCheck` or `using`
expression; a role-guard case cites the handler file and line and quotes the literal message and
status read from it. An empty `policies` array is recorded, in the document's scope line and in
the chat summary, as "no RLS policy matched this parser" — never as a statement that the project
has no permission boundaries, and never as a reason to skip the in-code pass, which is the
concrete failure 04-RESEARCH.md's Pitfall 3 verified against a real target repo with zero
policies. Where no role guard matched either, the rubric's own finding-nothing wording is used
rather than a new phrasing invented here.

State the `Ejecución` rule for permission cases as the third case of the existing bullet
(D-04): the layer is the layer the discovered check lives in — a route-handler role guard is
`API`, a page or form guard is `UI`, and an RLS policy is the `API` surface that writes the
table the policy is on, named as that surface rather than as the table. Decided here, so the
executor never re-infers it.

State the runnable-versus-pending decision (D-01, D-02). The permission case is always
generated, whether or not a second credential exists — never omitted, never a hard failure of
the run. Which `Ejecución` is written depends only on whether `QA_AGENT_TOKEN_SECONDARY` is
configured, checked with the exit-code-only presence check `## Configuration` documents, whose
output is discarded; reading the env file or printing the variable is forbidden there and is
forbidden here for the same reason. Configured means the layer value above; not configured means
the pending shape `references/test-case-format.md` publishes, written exactly as committed. If
that shape does not itself carry the layer, this protocol requires the layer to be named in the
case title so D-04's record of which layer the check lives in survives the pending state; if it
does carry the layer, say so and add nothing. Read the committed format contract to determine
which of the two applies before writing this bullet — do not write both.

Close the group with the D-05 bullet: the secondary user's role is never asked of the developer
up front. The case's expected result is written as the role boundary the discovered check states
— the guard's own message when the code announces one, the policy's predicate when only the
database does — and the actual role delta is inferred later, at run time, from the observed
difference between the two runs `## Running generated cases` performs. Generation records the
boundary that was found; it never asserts a permission model nobody wrote down.

In `scripts/discovery-surfaces.test.mjs`, add an exported `PERMISSION_GENERATION_MARKERS` array
to the `--- Pattern constants ---` block holding the distinctive literals this group must state,
including the empty-policies phrase, the secondary variable name, and the name of the rubric file
the protocol defers to. Add a `describe('case generation protocol — permission cases (anti-drift
lock)', ...)` block that reads `SKILL_PATH` once and asserts the skill text contains each marker,
following the existing agreement block's shape exactly — a rule the protocol no longer states
fails this test. Keep the file's header promise intact: nothing added here is imported by any
production script.
  </action>
  <verify>
    <automated>npm test -- scripts/discovery-surfaces.test.mjs</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'no RLS policy matched this parser' SKILL.md` is at least 1.
    - `grep -c 'PERMISSION_GENERATION_MARKERS' scripts/discovery-surfaces.test.mjs` is at least 2 (the exported constant and its lock assertion).
    - `npm test -- scripts/discovery-surfaces.test.mjs` exits 0 with no skipped tests, and its output includes the new permission-case agreement block.
    - `## Case generation protocol` states that both detection passes always run and that neither's empty result is evidence about the other.
    - `## Case generation protocol` states that at least one permission case is generated per policy record and per detected role guard.
    - `## Case generation protocol` states the `Ejecución` layer rule for all three permission-case sources (route-handler guard, page/form guard, RLS policy).
    - `## Case generation protocol` states that the runnable-versus-pending decision uses the presence-only check and names `QA_AGENT_TOKEN_SECONDARY`.
    - The pending shape named in the protocol is character-for-character the shape `references/test-case-format.md` publishes as committed by plan 04-04, and exactly one of the two layer-survival branches is written.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The orchestrator has a written, drift-locked rule that turns every discovered RLS policy and every discovered in-code role guard into at least one cited permission case, writes it runnable or pending from a presence check that cannot leak the credential, and reports finding nothing as finding nothing — closing DISC-05's core deliverable and 04-02's T-04-10 mitigation on the protocol side.</done>
</task>

<task type="auto">
  <name>Task 2: Type-implied, wrong-type and out-of-range case rules</name>
  <files>SKILL.md, scripts/discovery-surfaces.test.mjs</files>
  <read_first>
    - `SKILL.md` `## Case generation protocol` as committed by Task 1 — specifically the boundary-generation bullet plan 04-01 added (the four-case rule, the one-sided rule and the never-invent rule), which this task's type-implied rule must be readable beside without contradicting.
    - `references/test-case-format.md` as committed — `### Regla de límites (D-09/D-10)` from plan 04-01 and `### Subcategoría de caso (D-08)`, so the new rules use the titling and citation conventions already published rather than new ones.
    - `scripts/discover-schema.mjs` as committed — each constraint record's `type` field (the declared SQL type this task's rule reads), plus `bounds` and `allowedValues`.
    - `.planning/phases/04-edge-case-input-validation-quality/04-CONTEXT.md` — D-10, D-12, D-13, D-14 verbatim, and the Deferred Ideas section naming injection payloads as explicitly rejected.
    - `.planning/phases/04-edge-case-input-validation-quality/04-RESEARCH.md` — the Anti-Patterns bullet on not inventing a boundary when the parser returns null, and the Security Domain table.
    - `.planning/research/PITFALLS.md` — Pitfall 1 (evidence-backed reporting, no hallucinated success), which D-14 is the generation-time expression of.
  </read_first>
  <action>
Add a second group of bullets to `SKILL.md`'s `## Case generation protocol`, immediately after
the boundary-generation bullet plan 04-01 wrote, covering the three rules DISC-05 needs and
DISC-04's type dimension.

The type-implied rule (D-12): a numeric column whose declared SQL type implies a rejection
generates the odd values that type implies — zero, a negative value, and a decimal in an integer
column — even when no `CHECK` resolved any bound. Draw the distinction from D-10 sharply, because
the two rules look contradictory at a glance and are not: D-10 forbids inventing a *threshold*
nobody declared, while this reads a fact the column's own declared type states. Require the case
to cite the column type it was inferred from rather than a constraint expression, and require its
title to say the value is type-implied, so a reader can tell it apart from a case grounded in a
`CHECK`. A column whose type implies nothing generates none of these.

The wrong-type rule (D-13): out-of-range and wrong-type cases are strictly data-contract
violations — a string where a number is expected, a malformed JSON body, an array where an object
is expected, a missing required field's type counterpart. State the prohibition in the section's
own voice and make it unambiguous: no case generated by any rule in this protocol carries an
attack payload — no SQL-injection string, no script tag, no traversal sequence — because that is
security testing rather than QA-functional testing, and name the `security-audit` skill as where
that concern belongs. This is a scope boundary the developer chose, so state it as a boundary
rather than as an omission.

The expectation rule (D-14): a wrong-type or out-of-range case's `Resultado esperado` is the
generic `debe rechazar la request`, quoted in the protocol so the phrasing is fixed, and never a
specific HTTP status code the handler was not read to confirm. Say why in one clause: the real
code — 400, 422, 500, or an unvalidated pass-through — is captured as evidence at run time, and a
project that does not follow REST status conventions to the letter must not be handed a false
failure verdict for it. Note that a case whose status *was* read from the handler's own code is
the existing `negativo` path and keeps quoting that status verbatim — this rule governs the
inferred case, not the observed one.

In `scripts/discovery-surfaces.test.mjs`, add an exported `TYPE_AND_RANGE_MARKERS` array holding
this group's distinctive literals — the generic expected-result phrase, the name of the skill the
injection boundary points at, and the type-implied marker — and a
`describe('case generation protocol — type and range rules (anti-drift lock)', ...)` block
asserting the skill text contains each, in the same shape as Task 1's block.
  </action>
  <verify>
    <automated>npm test -- scripts/discovery-surfaces.test.mjs</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'debe rechazar la request' SKILL.md` is at least 1.
    - `grep -c 'security-audit' SKILL.md` is at least 1.
    - `grep -c 'TYPE_AND_RANGE_MARKERS' scripts/discovery-surfaces.test.mjs` is at least 2.
    - `## Case generation protocol` states the three type-implied odd values (zero, negative, decimal in an integer column) and requires the case to cite the column type rather than a constraint.
    - `## Case generation protocol` states the distinction between a type-implied value and an invented threshold, naming both rules so a reader cannot read them as contradictory.
    - `## Case generation protocol` states that no generated case carries an attack payload and names where that concern belongs instead.
    - `## Case generation protocol` states that an inferred rejection expectation is generic while a status read from the handler's own code is still quoted verbatim.
    - `npm test -- scripts/discovery-surfaces.test.mjs` exits 0 with no skipped tests.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The protocol generates the out-of-range and wrong-type coverage DISC-05 asks for, from facts the column's declared type states rather than thresholds nobody declared, with an expectation that cannot produce a false failure verdict and a scope boundary that keeps attack payloads out by rule rather than by habit.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch wiring — refuse a pending case, run the secondary pair, offer the run</name>
  <reversibility rating="costly">Step 4 is the single branch point every generated case passes through on its way to a real request; changing what it dispatches on later means changing the document contract, the reader and both executor protocols in lockstep, which is the same coupling 04-04 flagged for the `Ejecución` allowed set.</reversibility>
  <precondition>Plan 04-05's `--secondary` flag and plan 04-04's pending state are committed, so this step dispatches on values that exist and refuses a state the reader can actually parse.</precondition>
  <files>SKILL.md, scripts/discovery-surfaces.test.mjs</files>
  <read_first>
    - `SKILL.md` `## Running generated cases` in full — all six steps and the closing note, especially step 3's reader-CLI rule and its warning about a short ID being a substring of a longer one, step 4's two dispatch branches, step 5's confirmation-still-applies rule and its `FORBIDDEN_DISPATCH_FLAGS` explanation, and step 6's report rule.
    - `SKILL.md` `## Case generation protocol`'s closing terminal-step bullet as committed by plans 04-01 and Task 1 — the bullet D-07's offer amends.
    - `SKILL.md` `## Case construction` as committed by plan 04-05 — the `--secondary` bullet this step points at rather than restating.
    - `04-04-SUMMARY.md`, `references/test-case-format.md` and `scripts/test-case-doc.mjs` as committed — the exact pending shape and the exact field the reader's `--case` JSON exposes it on (`ejecucion`, or the `pendiente`/`pendienteMotivo` pair, depending on the option selected at 04-04's checkpoint); the refusal must key on the field that exists.
    - `04-05-SUMMARY.md` and `scripts/api-client.mjs` as committed — the `--secondary` flag, the exit-2 refusals, and `evidence.request.auth.credential`.
    - `.planning/phases/04-edge-case-input-validation-quality/04-CONTEXT.md` — D-02, D-05, D-06, D-07 verbatim.
    - `scripts/discovery-surfaces.test.mjs` as committed by Tasks 1 and 2 — the two marker arrays and lock blocks this task's third one follows.
  </read_first>
  <action>
Rewrite step 4 of `SKILL.md`'s `## Running generated cases` so it branches three ways instead of
two, keeping the existing `API` and `UI` branches' text unchanged in substance.

Add the pending branch first, before the two dispatch branches, so the refusal is read before
either construction rule. A case the reader reports as pending is not dispatched: report the
document's own reason text verbatim, name `QA_AGENT_TOKEN_SECONDARY` as what is missing and
`## Configuration` as where to set it, and stop for that case — then continue with the other
named cases rather than aborting the whole run. State the three things this refusal is not: it is
not a test failure and must never be rendered as one, it is not a silent skip (the developer is
told, by case ID, which cases the run could not execute), and it is not something to work around
by substituting the primary credential, which `api-client.mjs` would refuse at exit 2 anyway.
Name the field the reader's `--case` JSON actually exposes the pending state on, as committed by
plan 04-04 — key the refusal on that field rather than on the raw document text, for the same
reason step 3 already forbids grepping the document by eye.

Extend the `API` branch with the secondary-role rule. A permission case whose steps describe
acting as the second, lower-privilege user is dispatched with `--secondary` added exactly as
`## Case construction` specifies — never with the credential as an argument, never together with
`--storage-state`. State D-05's two-run rule plainly: to learn the role delta, dispatch the same
case twice, once without the flag and once with it, and compare the two captured results' status
codes and response shapes. Each result's `evidence.request.auth.credential` names which
credential produced it, so the comparison is between two recorded runs rather than between a
recorded run and a remembered one. The delta is reported as what was observed — "the secondary
credential got 403 where the primary got 201" — never as a claim about a permission model, which
is D-05's inference-from-observation discipline and the same stance the `Tipo` rule already takes
for an inferred expectation.

Leave step 5 untouched in substance and add one clause making its reach over this phase's new
case types explicit: a permission case whose steps describe a destructive action stops and asks
exactly as any other case does, and the `FORBIDDEN_DISPATCH_FLAGS` refusal covers the pending
case's own block with no exception. Do not weaken or duplicate the existing rule — it already
holds; this only names the new case types it holds over.

Then wire D-07's offer in two places, together, so the two sections cannot contradict each other.
In `## Case generation protocol`'s closing terminal-step bullet, add that the chat summary may
close by offering to run the newly generated cases — an offer in the form of a question naming
the case count, such as `¿Corro estos N casos ahora?` — and that this changes nothing about the
terminal-step rule itself: the offer is a question, the developer must still answer yes, and no
case is ever run in the same turn it was generated. In `## Running generated cases`'s closing
note, add the matching clause: a run that begins because the developer accepted that offer is an
ordinary run of this section from step 1, with the same confirmation gates, and it is a new turn.
State the pending count in the offer separately from the runnable count, so a developer is never
offered a number that includes cases the run would refuse.

In `scripts/discovery-surfaces.test.mjs`, add an exported `PENDING_DISPATCH_MARKERS` array
holding this task's distinctive literals — the offer's question form, the secondary flag, and the
pending literal as committed by plan 04-04 — and a `describe('running generated cases — pending
refusal and secondary dispatch (anti-drift lock)', ...)` block asserting the skill text contains
each, in the same shape as Tasks 1 and 2.
  </action>
  <verify>
    <automated>npm test -- scripts/discovery-surfaces.test.mjs</automated>
    <automated>npm test -- scripts/test-case-doc.test.mjs</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PENDING_DISPATCH_MARKERS' scripts/discovery-surfaces.test.mjs` is at least 2.
    - `grep -c '¿Corro estos' SKILL.md` is at least 1.
    - `grep -c -- '--secondary' SKILL.md` is at least 4 (plan 04-05's three occurrences plus this step's dispatch rule).
    - `## Running generated cases` step 4 has three branches, with the pending refusal written before the two dispatch branches.
    - The pending branch names `QA_AGENT_TOKEN_SECONDARY`, states the case is reported by ID rather than silently skipped, states it is never rendered as a test failure, and states the run continues with the remaining named cases.
    - The pending refusal keys on the field `scripts/test-case-doc.mjs`'s `--case` JSON actually exposes, as committed by plan 04-04.
    - The `API` branch states the two-run role-delta rule and names `evidence.request.auth.credential` as what distinguishes the two results.
    - `## Case generation protocol`'s closing bullet and `## Running generated cases`'s closing note both state that accepting the run offer is a new turn and that no case runs in the turn it was generated.
    - `npm test` exits 0 with no skipped tests.
  </acceptance_criteria>
  <done>A generated permission case has exactly one honest path forward in every configuration: runnable as the second user through a flag that cannot carry the credential, pending and refused by name with the missing variable stated, or refused at the confirmation gate like any other destructive case — and the developer is offered the run in the same response without a case ever executing in the turn it was written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target project `.env.local` presence check → orchestrator | The orchestrator must learn one bit (configured or not) about a secret without observing its value |
| discovered policy records and role guards → generated permission cases | A parser's or rubric's silence becomes a document whose gaps a developer may read as coverage |
| generated document → `## Running generated cases` step 4 → a real request | A case's parsed state decides whether, how, and as whom a request is sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Elevation of Privilege | dispatch of a pending permission case | high | mitigate | Step 4's pending branch is read before either dispatch branch and keys on the field the reader's `--case` JSON exposes, so a pending case is refused by name and never reaches `api-client.mjs`; plan 04-05's exit-2 refusal is the independent second tier if a hand-edited document is run anyway, and the protocol forbids substituting the primary credential as a workaround |
| T-04-16 | Information Disclosure | the generation-time presence check of `QA_AGENT_TOKEN_SECONDARY` | high | mitigate | The protocol requires the exit-code-only check `## Configuration` documents, with output discarded, and forbids `Read`ing the env file or running anything that prints the variable — the orchestrator learns one bit and the value never enters the transcript (04-RESEARCH.md Pitfall 4); the `PERMISSION_GENERATION_MARKERS` lock fails if the protocol stops naming the variable and the rule |
| T-04-17 | Repudiation | an empty `policies` array or an unmatched role-guard pass | high | mitigate | The protocol requires the empty result be reported as "no RLS policy matched this parser", requires both passes to run independently of each other's result, and requires each generated case to name the pass that found it and cite its file and line — so silence is readable as silence, closing plan 04-02's T-04-10 and plan 04-03's T-04-11 mitigations on the protocol side (04-RESEARCH.md Pitfall 3) |
| T-04-18 | Tampering | a generated case's `Pasos` | high | mitigate | D-13's rule forbids any attack payload in any generated case by name (SQL injection, script tags, traversal sequences) and points at the `security-audit` skill instead, so the boundary is a stated rule with a drift lock rather than an implicit habit; step 5's `FORBIDDEN_DISPATCH_FLAGS` refusal and confirmation gate continue to cover every new case type unchanged |
| T-04-20 | Spoofing | a role-delta conclusion drawn from two runs that cannot be told apart | medium | mitigate | The two-run rule requires each result's `evidence.request.auth.credential` (plan 04-05) to identify the credential, and requires the delta be reported as the observed status/shape difference rather than as a permission-model claim, so a mislabelled pair surfaces as a missing field rather than a confident wrong verdict |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages — 04-RESEARCH.md's Package Legitimacy Audit records the gate as not applicable for this phase (all work edits documentation and one existing test file) |
</threat_model>

<verification>
- `npm test` is green (full suite, including the three new anti-drift lock blocks).
- `SKILL.md`'s `## Case generation protocol` generates permission cases from both detection passes, cites each case's source, and reports an empty policy result honestly.
- `SKILL.md`'s `## Running generated cases` step 4 refuses a pending case by name before either dispatch branch, and dispatches a secondary-role case through `--secondary`.
- Every literal the three marker arrays name is present in `SKILL.md`, and removing any one of them fails `scripts/discovery-surfaces.test.mjs`.
- The pending shape and the reader field this plan names match what plan 04-04 actually committed.
</verification>

<success_criteria>
DISC-05's permission cases are actually generated — one or more per discovered RLS policy and per discovered in-code role guard, each cited, each written runnable or pending from a presence check that cannot leak the credential — and each has exactly one honest path at dispatch: run as the second user, or be refused by name with the missing variable stated. DISC-04's type dimension and DISC-05's wrong-type/out-of-range coverage are generated under rules that never invent a threshold, never carry an attack payload, and never assert a status code nobody read.
</success_criteria>

<output>
Create `.planning/phases/04-edge-case-input-validation-quality/04-06-SUMMARY.md` when done
</output>
